# Docker command line sheet

- `$ docker --help` to list all command
- `$ docker [command] --help` to see the detail

| Command           | Describe                                                   |
| ----------------- | ---------------------------------------------------------- |
| `$ docker ps`     | List containers                                            |
| `$ docker ps -a ` | -a, --all Show all containers (default shows just running) |

| `$ docker run
